sudo: required

language: node_js
node_js:
  - "7"
services:
  - docker

before_install:
  - docker pull alexcheng/magento2
  - docker-compose up -d
  - sudo ls /var/www/html
  - docker exec -it install-magento

script:
  - npm test